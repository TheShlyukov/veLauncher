Before running program sourse code, install dependencies via this command:

pip install -r requirements.txt

This is pre alpha, so launcher is not compiled yet.

if you have trouble with PyQt, use veLauncher_classic (you can get it from head of project)

Supports: Windows, MacOS, Linux.

By ShLKV (The_Shlyukov) :D
